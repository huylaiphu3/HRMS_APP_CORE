# Database
DB_ROOT_PASSWORD=change_me_strong_password
DB_NAME=hrms
DB_PORT=3306

# JWT
JWT_SECRET=change_me_at_least_256_bits_random_string_for_hmac_sha256

# Encryption (AES-256-GCM for CCCD field)
ENCRYPTION_KEY=change_me_exactly_32_characters!!

# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# SMTP (dev: mailhog on 1025, prod: real SMTP)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# File Upload
FILE_UPLOAD_DIR=./uploads
